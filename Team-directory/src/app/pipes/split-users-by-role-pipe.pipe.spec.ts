import { SplitUsersByRolePipePipe } from './split-users-by-role-pipe.pipe';

describe('SplitUsersByRolePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitUsersByRolePipePipe();
    expect(pipe).toBeTruthy();
  });
});
